import React, {useState, useEffect} from 'react';
import axios from 'axios';
 
const Reg = () => {
    const [movie, setMovie] = useState([]);

    useEffect(() => {
        
        return fetchData = () => {
            axios.get()
        }
    }, [input])
    return (
        <>
            <div>

            </div>
        </>
    )
}

export default Reg;